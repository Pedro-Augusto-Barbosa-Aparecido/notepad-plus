export const IPC = {
  FRAME_EVENTS: {
    CLOSE: 'frame-events: close',
    MAXIMIZE: 'frame-events: maximize',
    MINIMIZE: 'frame-events: minimize',
    RESIZE: 'frame-events: minimize'
  },
  FILE_EVENTS: {
    OPEN: 'file-events: open'
  }
} as const
